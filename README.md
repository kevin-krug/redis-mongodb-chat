# Chat-App

simple web based chat app with redis cache and message persistance to mongodb. 

## Setup

`yarn`

##  Run Locally
 
`yarn run start:redis`

starts local redis instance

`yarn run debug`

runs server.js in debug mode with auto-reload on file changes

## Run with Docker

`docker-compose up` or `podman compose up`

##  License

ISC